add apt-get install silversearcher-ag to list
pacman -S the_silver_searcher

error for archlinux with cpsm
https://github.com/nixprime/cpsm/pull/36

yaourt -S xf86-input-mtrack-git
mtrack symlink

* # E.g. On Arch:
* gesture control for libinput
* sudo pacman -S xdotool wmctrl
*
* yaourt libinput-gestures
*
* add env $USER
* sudo gpasswd -a $USER input
